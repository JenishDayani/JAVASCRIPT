const score1 = 90;
const score2 = 60;
const score3 = 60;
const total = score1 + score2 + score3;
const average = total / 3;

console.log('Total is ' + total);
console.log('Average is ' + average);

const plates = 20;
const people = 7;
const remaing = plates % people;
const remain = remaing + 1;

console.log('There are ' + remain + ' plates available');
